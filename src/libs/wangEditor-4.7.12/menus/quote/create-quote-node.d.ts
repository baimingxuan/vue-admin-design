import { DomElement } from '../../utils/dom-core';
/**
 * 创建一个blockquote元素节点
 * @param editor 编辑器实例
 */
declare function createQuote($childElem: DomElement[]): DomElement;
export default createQuote;
